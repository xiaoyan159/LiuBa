<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="ring"
    android:thickness="2dp"
    android:innerRadius="@dimen/title_font_size"
    android:useLevel="false">
    <padding
        android:bottom="@dimen/primary_padding"
        android:left="@dimen/primary_padding"
        android:right="@dimen/primary_padding"
        android:top="@dimen/primary_padding"></padding>
    <!-- 填充 -->
    <solid android:color="@android:color/white" /><!-- 填充的颜色 -->
</shape>
